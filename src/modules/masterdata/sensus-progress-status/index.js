import SensusProgressStatusView from './view.vue'

export default {
  routes: [
    {
      path: '/master-data/sensus-progress-status',
      name: 'SensusProgressStatus',
      component: SensusProgressStatusView
    }
  ]
}
